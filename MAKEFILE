# Makefile – Stockly-TagForge

#Note: This Makefile is generated by CHATGPT based on a previous version makefile version
#Note: This Makefile is used to manage the Python project environment, dependencies, and build process.
#Note: WARNING !!!!!!! Currently, this makefiles is not tested on Windows, Linux, or macOS use at your own risk !!!!!!!!!!!


# Variables
PYTHON := python3
PIP    := $(PYTHON) -m pip
VENV   := .venv
REQ    := requirements.txt
MAIN   := main.py

# Targets d'environnement
.PHONY: all venv install clean launch build

all: install

venv: $(VENV)/bin/activate

$(VENV)/bin/activate: $(REQ)
	$(PYTHON) -m venv $(VENV)
	$(PIP) install --upgrade pip setuptools
	$(PIP) install -r $(REQ)

install: venv
	@echo "✅ Environnement prêt."

# Lancer l'application
launch: install
	$(VENV)/bin/$(PYTHON) $(MAIN)

# Nettoyage des caches
clean:
	rm -rf __pycache__ $(VENV)
	find . -name '*.pyc' -delete

# Créer un binaire unique (PyInstaller)
build: install
	$(VENV)/bin/pyinstaller --onefile --windowed $(MAIN)



#Note: Yes this makefile used french and english comments, but i do what i want, this is my makefile !!!!!!
